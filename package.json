{
  "name": "string-tree",
  "main": "string-tree.js",
  "version": "0.6.0",
  "description": "Navigate a tree of unique strings",
  "devDependencies": {
    "run-test": "*"
  },
  "scripts": {
    "test": "node test.js"
  }
}
